#include<stdio.h>
int main()
{
  int x,y,temp;
  printf("enter the values x and y");
  scanf("%d %d,&x,&y);
  printf("before swapping x=%d,y=%d",x,y);
  temp=x;
  y=temp;
  printf("after swapping x=%d,b=%d",x,y);
  return 0;
}
